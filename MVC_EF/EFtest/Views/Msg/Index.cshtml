@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        <table border="1">
            <tr>
                <th>列1</th>
                <th>列2</th>
                <th>列3</th>
            </tr>
            @foreach (var ba in Model)
            {
                var b = ba as EFtest.Models.MsgTypeViewModel;
                <tr>
                    <td>@b.Id</td>
                    <td>@b.Type</td>
                </tr>
            }
            @foreach (var ba in Model)
            {
                var b = ba as EFtest.Models.MsgTypeViewModel;
                <tr>
                    <td>@b.Id</td>
                    <td>@b.Type</td>
                </tr>
            }
        </table>
    </div>
</body>
</html>
