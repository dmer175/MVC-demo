@model EFtest.Models.Messages
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit</title>
</head>
<body>
    <div>
        @using (Html.BeginForm("Edit","Update",FormMethod.Post))
        {
            <span>Id</span>
            @Model.Id
            @Html.HiddenFor(m=>m.Id)
            <br />
            <span>内容</span>
            @Html.TextBoxFor(m=>m.Messages1)
            <br />
            <input type="submit" value="修改" />
        }
    </div>
</body>
</html>
